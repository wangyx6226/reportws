

const mutations = {
  
  SET_LANGRAGE(state, lang) {
    state.langrage = lang
  },

  SET_USER_INFO(state, userinfo) {
    state.userInfo = userinfo
  },

  SET_CP_LIST(state, list) {
    state.cpList = list
  },

  SET_SP_LIST(state, list) {
    state.spList = list
  },

  SET_CHANNEL_LIST(state, list) {
    state.channelList = list
  },

  
}
export default mutations